# Copy to .env only if you add your own env loader/process manager.
# Railway variables should be configured in the Railway dashboard/service settings.
ADMIN_USERNAME=admin
ADMIN_PASSWORD=replace-with-a-long-random-password
STORAGE_DIR=/data
NODE_ENV=production
