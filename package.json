{
  "name": "facebook-resume-cms",
  "version": "2.3.2",
  "private": true,
  "description": "Facebook-inspired resume portfolio with a secure admin CMS, uploads, searchable public profile, and Railway-ready persistent storage.",
  "scripts": {
    "start": "node hydrate-assets.js && node server.js",
    "dev": "node hydrate-assets.js && node --watch server.js"
  },
  "engines": {
    "node": ">=20"
  }
}
